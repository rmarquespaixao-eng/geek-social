---
title: PATCH /chat/conversations/{id}/temporary
description: Ligar/desligar modo temporário (per-user)
---

import Meta from '@/generated/api/chat/set-temporary/meta'
import Request from '@/generated/api/chat/set-temporary/request'
import Responses from '@/generated/api/chat/set-temporary/responses'
import Errors from '@/generated/api/chat/set-temporary/errors'
import Examples from '@/generated/api/chat/set-temporary/examples'

<Meta />

Quando enabled=true, mensagens enviadas por mim ficam efêmeras pro outro lado.

## Request

<Request />

## Response

<Responses />

## Erros

<Errors />

{/* TODO: para cada código de erro acima, explique a causa e como resolver. */}

## Exemplos

<Examples />


## Side effects

{/* TODO: eventos de socket emitidos, notificações disparadas, jobs enfileirados, cascades. */}

## Relacionados

{/* TODO: links pra módulo, conceitos, endpoints relacionados. */}
