---
title: GET /collections/{collectionId}/items
description: Listar itens da coleção
---

import Meta from '@/generated/api/items/list/meta'
import Request from '@/generated/api/items/list/request'
import Responses from '@/generated/api/items/list/responses'
import Errors from '@/generated/api/items/list/errors'
import Examples from '@/generated/api/items/list/examples'

<Meta />

Cursor pagination + filtros dinâmicos baseados em fields da coleção. Query params: q, cursor, limit (1-100, default 30), sort (recent|oldest|name|name_desc|rating), rating_min (1-5), has_cover (true|false), field_KEY (igualdade ou CSV em select), field_KEY_gte/_lte (range em number/money/date).

## Request

<Request />

## Response

<Responses />

## Erros

<Errors />

{/* TODO: para cada código de erro acima, explique a causa e como resolver. */}

## Exemplos

<Examples />


## Side effects

{/* TODO: eventos de socket emitidos, notificações disparadas, jobs enfileirados, cascades. */}

## Relacionados

{/* TODO: links pra módulo, conceitos, endpoints relacionados. */}
