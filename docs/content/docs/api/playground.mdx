---
title: Sandbox interativo
description: Teste qualquer endpoint da API direto no navegador.
full: true
---

import { Scalar } from '@/components/Scalar'

Sandbox interativo da API completa — navegue entre endpoints, faça requests reais, veja responses ao vivo. Use seu **access token** (obtenha via `POST /auth/login`) no botão "Auth" do Scalar.

<Scalar operationId="" />
